#!/usr/bin/env bash
set -euo pipefail
# Replace this placeholder with the project's actual build and test commands.
# Use RUNHQ_WORKSPACE_ROOT for the code; this script's cwd is the captured package.
printf '%s\n' 'Configure verify.sh with the required project checks before running this example.' >&2
exit 2
